package types

const (
	Name        = "lets-go-tls"
	NameMetrics = "lets_go_tls"

	NameServer        = Name + "-server"
	NameServerMetrics = NameMetrics + "_server"

	NameAgent        = Name + "-agent"
	NameAgentMetrics = NameMetrics + "_agent"
)
